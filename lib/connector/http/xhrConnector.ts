
import { Config, QueueRequest } from "../../types";

export default function xhrConnector(config: Config, retryRequest?: QueueRequest) {
    return new Promise((resolvePromise, rejectPromise) => {

    });
}

module.exports = xhrConnector;
